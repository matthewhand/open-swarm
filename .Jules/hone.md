## 2024-06-15 | [Architectural Audit] | Insight: Missing focus traps in modals | Protocol: Wrap modal contents with `focus-trap-react` and manage native dialog open states deterministically.
## 2024-06-15 | [Architectural Audit] | Insight: Silent async states | Protocol: Apply `aria-live="polite"`, `aria-busy="true"`, and `role="status"` on Loading, Error, and Empty states of network-dependent components.
## 2024-06-15 | [Architectural Audit] | Insight: Anonymous default exports | Protocol: Assign objects to a named variable before `export default` to adhere to modern ESLint rules and maintain strict type-safety standards.
## 2024-07-01 | [Architectural Audit] | Insight: Missing native Dialog closing focus | Protocol: Manage `document.activeElement` within an internal `ref` before calling `showModal()`, and explicitly focus it when the native `dialog.close()` fires.
## 2024-07-01 | [Architectural Audit] | Insight: Loading indicators lack specific roles | Protocol: Ensure all visual spinners and loaders use explicit `role="status"` rather than bare attributes to announce themselves correctly via AT.
## 2024-07-01 | [Architectural Audit] | Insight: Unhandled state in generic infinite scroll hooks | Protocol: Introduce `<T>` typing to custom hooks like `useInfiniteScroll` to guarantee type safety, and rigorously expose deterministic `error` and `isEmpty` boolean states to guarantee predictable downstream A11y and UX fallbacks.
