## 2024-05-24 | [Architectural Audit] | Insight: Testing Library Anti-patterns | Protocol: Ensure proper Testing Library methods are used instead of querySelector/querySelector and DOM node traversal. Use screen.getByRole, screen.getByText, and expect to match classes using toHaveClass and toHaveAttribute instead.

## 2024-05-24 | [Architectural Audit] | Insight: Missing State Accessibility Attributes | Protocol: Ensure all network-dependent wrapper states including loading placeholders (`SkeletonCard`, `LoadingSpinner`) and error blocks (`Alert`) are deterministically wrapped with `aria-live` and `aria-busy` and explicit `role="alert"` or `role="status"` where required. In `AgentCreatorPage` and `BuilderPage`, data fetch results lacked such structural ARIA states which I have corrected.

## 2024-05-24 | [Architectural Audit] | Insight: Keyboard Navigation Coverage on Tab / Modal | Protocol: DaisyUI's base Tabs lacked Home/End arrow key operability, which strictly breaks accessibility specs for complex tablists. DaisyUI Modals require strict focus restoration; they didn't manually cache `document.activeElement`. We have updated `<Tabs>` to natively handle complex directional interactions skipping disabled nodes, and updated `<Modal>` to perform robust focus restoration.
