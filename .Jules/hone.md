## 2024-06-15 | [Architectural Audit] | Insight: Missing focus traps in modals | Protocol: Wrap modal contents with `focus-trap-react` and manage native dialog open states deterministically.
## 2024-06-15 | [Architectural Audit] | Insight: Silent async states | Protocol: Apply `aria-live="polite"`, `aria-busy="true"`, and `role="status"` on Loading, Error, and Empty states of network-dependent components.
## 2024-06-15 | [Architectural Audit] | Insight: Anonymous default exports | Protocol: Assign objects to a named variable before `export default` to adhere to modern ESLint rules and maintain strict type-safety standards.
## 2024-07-17 | [Architectural Audit] | Insight: Insecure generic DOM queries breaking focus isolation | Protocol: Use isolated `useRef` hooks arrays (e.g. `useRef<(HTMLElement | null)[]>([])`) to track dynamically rendered elements for robust keyboard navigation and isolation, avoiding `document.getElementById()`.
## 2024-07-17 | [Architectural Audit] | Insight: Unfocusable HTML5 dialog backdrops intercepting events | Protocol: DaisyUI's `modal-backdrop` should always use a `<button>` tag instead of an unfocusable `<form method="dialog">` so it can handle clicks accessibly.
## 2024-07-17 | [Architectural Audit] | Insight: Implicit `any` typings obscuring errors in fetch promises | Protocol: Replace inferred `any` generic parameterizations in API maps and error catches with strict `unknown` accompanied by safe type guards like `e instanceof Error` or manual validation.
## 2024-07-17 | [Architectural Audit] | Insight: Undefined A11y states in custom Pagination loops | Protocol: Elements representing current selections must utilize `aria-current="page"` (or similar appropriate attribute) in addition to visible active classes to communicate deterministic state to screen readers.
