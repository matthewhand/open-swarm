## 2024-07-11 | [Architectural Audit] | Insight: Brittle Keyboard Focus in Custom React Components | Protocol: Always use virtual-DOM-aware `useRef` maps/arrays instead of native `document.getElementById` to ensure robust, encapsulated A11y.
## 2024-07-11 | [Architectural Audit] | Insight: Missing State Notifications in Async Operations | Protocol: Deterministic UI states (Loading, Success, Error, Empty) must be accompanied by `aria-live="polite"`, `aria-busy="true"`, or `role="status"` to notify screen readers of DOM updates dynamically.
## 2024-07-11 | [Architectural Audit] | Insight: CSS Transition Loss on Modal Unmount | Protocol: When managing focus traps for native HTML5 `<dialog>` modals, use the trap's `active` prop instead of unmounting the trap component, allowing native CSS close transitions to complete.
