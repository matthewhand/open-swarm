## 2024-08-20 | [Architectural Audit] | Insight: Modal Form Dialog Backdrop | Protocol: Utilize native `<form method="dialog">` for modal backdrops to allow the browser to natively handle modal closing semantics, rather than full-screen buttons with JavaScript bounding rects.
## 2024-08-20 | [Architectural Audit] | Insight: Accordion Semantic HTML | Protocol: Refactor custom `div`/`input[type="checkbox"]` accordions to use HTML5 semantic `<details>` and `<summary>` elements to guarantee native keyboard focusability and ARIA accessibility.
## 2024-08-20 | [Architectural Audit] | Insight: Form Double Submission & Error State Integrity | Protocol: Ensure all network-dependent submit components expose a deterministic error state string, and lock operations with `isSubmitting` guards before processing callbacks.
