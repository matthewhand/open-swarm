## 2024-07-08 | [Architectural Audit] | Insight: Native DOM traversal (e.g., `document.getElementById`) inside React components invites UX and accessibility regressions, particularly in virtual-DOM environments. | Protocol: Replaced native DOM traversals with a map of React `useRef` hooks in `Tabs.tsx` to maintain robust, virtual-DOM-aware keyboard focus management.
## 2024-07-08 | [Architectural Audit] | Insight: Conditionally rendering native `dialog` wrappers breaks CSS transitions and focus trap lifecycle synchronization. | Protocol: Adjusted `Modal.tsx` to always render the `FocusTrap` wrapper and conditionally control its focus-trap state via the `active={isOpen}` prop. Avoided redundant `role="dialog"` attributes to prevent screen-reader noise.
## 2024-07-08 | [Architectural Audit] | Insight: Systemic anti-pattern of using native browser `confirm()` dialogues introduces blocking, non-accessible modal experiences that fail a11y focus management. | Protocol: Refactored `TeamsPage.tsx` to utilize the DaisyUI `ConfirmModal`, instituting a deterministic state-machine (`teamToDelete`) to securely handle pending deletions.
