## 2024-06-15 | [Architectural Audit] | Insight: Missing focus traps in modals | Protocol: Wrap modal contents with `focus-trap-react` and manage native dialog open states deterministically.
## 2024-06-15 | [Architectural Audit] | Insight: Silent async states | Protocol: Apply `aria-live="polite"`, `aria-busy="true"`, and `role="status"` on Loading, Error, and Empty states of network-dependent components.
## 2024-06-15 | [Architectural Audit] | Insight: Anonymous default exports | Protocol: Assign objects to a named variable before `export default` to adhere to modern ESLint rules and maintain strict type-safety standards.
## 2024-06-26 | [Architectural Audit] | Insight: Native HTML5 dialog `<dialog>` usage caused missing focus restoration upon closing, violating WAI-ARIA standards. | Protocol: Enforce manual capture of `document.activeElement` on Modal mount and manually restore `.focus()` on unmount to trap and return focus cleanly.
## 2024-06-26 | [Architectural Audit] | Insight: DaisyUI Tabs lacked `Home` and `End` keyboard mappings, violating structural a11y keyboard operability limits. | Protocol: Bound ArrowLeft/ArrowRight to previous/next enabled elements, and added explicit limits on Tab keys with wrap-around boundaries for `Home` and `End`.
## 2024-06-26 | [Architectural Audit] | Insight: Complex asynchronous components (e.g. `useInfiniteScroll`) lost determinism using `any` types and lacked exposed `error` or `isEmpty` states, leading to inconsistent UI handling. | Protocol: Rewrote the hooks exclusively using TypeScript generics `<T>` and explicitly declared strict UI state permutations.
## 2024-06-26 | [Architectural Audit] | Insight: DaisyUI 5's bare class loading indicators missed explicit `aria-live` and `aria-busy` roles, yielding silent or brittle announcements to screen readers. | Protocol: Required manual propagation of `aria-live="polite"` and `aria-busy="true"` dynamically inside base React `span` wrappers for loading components.
