## 2025-01-20 | [Architectural Audit] | Insight: Conditional FocusTrap Unmounting Breaks Transitions | Protocol: Prefer using the `active` prop on FocusTrap to control its state instead of mounting/unmounting it, ensuring DaisyUI CSS transitions on close are preserved while maintaining A11y keyboard operability.

## 2025-01-20 | [Architectural Audit] | Insight: Missing ARIA Regions on Async State Changes | Protocol: Ensure all network-dependent components have deterministic UI states (Loading, Error, Empty, Success) paired with appropriate ARIA attributes. Use `aria-busy="true"` and `aria-live="polite"` for loading states, `<div aria-live="assertive">` for error states, and `role="status"` for empty states to guarantee screen-reader announcements.

## 2025-01-20 | [Architectural Audit] | Insight: Overuse of `any` Type and Brittle React Hook Dependencies | Protocol: Enforce strict TypeScript types by leveraging generics (`<T>`) rather than falling back to `any`. Maintain referential equality in React Hooks by moving inline logical expressions and array initializations (e.g., `?? []`) inside `useMemo` callbacks to prevent infinite re-renders and exhaustive-deps warnings.