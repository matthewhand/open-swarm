## 2024-07-20 | [Architectural Audit] | Insight: Direct DOM manipulation in Tabs keyboard navigation bypasses React's virtual DOM and ref system, leading to brittle accessibility. | Protocol: Refactored Tabs to use an array of `useRef` for focus management, ensuring React-compliant keyboard operability (Left/Right/Home/End).
## 2024-07-20 | [Architectural Audit] | Insight: HTML5 `<dialog>` elements in DaisyUI use `<form method="dialog">` for backdrops, which are unfocusable and swallow React synthetic events. | Protocol: Replaced `<form method="dialog">` backdrops with a semantic `<button>` element to ensure correct focus trapping and backdrop interactions.
## 2024-07-20 | [Architectural Audit] | Insight: Async operations in the Dashboard component lack typed error handling (using `any`) and do not deterministically expose ARIA states (aria-live/aria-busy). | Protocol: Implemented explicit `unknown` error typing with `instanceof Error` checks, and structured all network-dependent states into deterministic loading, error, and success modes with proper `role="alert"` and `aria-busy` bindings.
