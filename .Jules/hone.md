## 2024-06-26 | [Architectural Audit] | Insight: DaisyUI 5 `<dialog>` component requires `.modal-open` class for testing, but in JSDOM, native properties like `.open` or `showModal()` are unreliable. | Protocol: Test visibility using DaisyUI classes and fallbackFocus when testing with JSDOM.
## 2024-06-26 | [Architectural Audit] | Insight: DaisyUI Buttons use an implicit class approach instead of explicit spinner elements, which breaks screen readers and leads to testing flakiness. | Protocol: Refactored `Button` components to explicitly manage standard DaisyUI spinner classes for correct loading state representation and `aria-live`.
## 2024-06-26 | [Architectural Audit] | Insight: Incomplete type definition array typing leads to missing types and usage of `any` types. | Protocol: Refactored to define the Array `<T>` for missing typed lists ensuring generic structural typing instead of generic dynamic typings.
