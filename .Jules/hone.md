## 2025-06-22 | [Architectural Audit] | Insight: Missing Manual Focus Restoration on Modals | Protocol: Modal components must capture the `document.activeElement` before opening and manually call `.focus()` on it when the native `<dialog>` closes, preventing focus from being dropped to the `<body>` on exit.
## 2025-06-22 | [Architectural Audit] | Insight: Unstable Dependency Arrays in Frontend Hooks | Protocol: useMemo should encapsulate the fallback of empty arrays to avoid returning a new array reference every render (e.g., `info?.tools.mcp_catalog ?? []` causing `react-hooks/exhaustive-deps` warnings when placed inline).
## 2025-06-22 | [Architectural Audit] | Insight: Native Node DOM Access in React Tests | Protocol: Test files (`Button.test.tsx`, etc) should not use `container.querySelector` or direct DOM access; `screen.getByRole` or `screen.getByTestId` with React Testing Library queries must be used exclusively to enforce accessible querying patterns and resolve lint errors (`testing-library/no-node-access`).
## 2025-06-26 | [Architectural Audit] | Insight: Strict Mode Violations in Playwright E2E Tests | Protocol: E2E tests using `getByLabel` are brittle when ARIA labels map to both a parent `<label>` and a child `<input>`. Ensure that E2E visual tests (e.g., `test_golden_journey.py` and `interaction.spec.ts`) bypass strict mode violations by appending `.first()` when using robust attribute selectors like `page.locator('[aria-label="Toggle dark mode"]').first().click(force=True)`.
