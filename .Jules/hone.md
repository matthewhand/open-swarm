## 2024-06-15 | [Architectural Audit] | Insight: Missing focus traps in modals | Protocol: Wrap modal contents with `focus-trap-react` and manage native dialog open states deterministically.
## 2024-06-15 | [Architectural Audit] | Insight: Silent async states | Protocol: Apply `aria-live="polite"`, `aria-busy="true"`, and `role="status"` on Loading, Error, and Empty states of network-dependent components.
## 2024-06-15 | [Architectural Audit] | Insight: Anonymous default exports | Protocol: Assign objects to a named variable before `export default` to adhere to modern ESLint rules and maintain strict type-safety standards.
## 2024-06-30 | [Architectural Audit] | Insight: Loading spinners in buttons incorrectly rely on `aria-hidden` and `sr-only` text rather than native ARIA status roles. | Protocol: Refactor `LoadingSpinner` components and DaisyUI `Button` elements to directly output `<span role="status" aria-label="Loading" />` and update tests to query by role.
## 2024-06-30 | [Architectural Audit] | Insight: Async data fetch boundaries (React-Query `isPending`, `isError`) lack semantic live regions. | Protocol: Implement `aria-live="polite"` and `aria-busy="true"` on pending state containers, and wrap error alerts in `role="alert"` with `aria-live="assertive"`.
## 2024-06-30 | [Architectural Audit] | Insight: Component initializations falling back to inline arrays inside `useMemo` dependencies trigger `react-hooks/exhaustive-deps`. | Protocol: Move logical initializations inside the `useMemo` callback function to ensure correct referential equality and avoid infinite render loops.
