## 2024-06-15 | [Architectural Audit] | Insight: Missing focus traps in modals | Protocol: Wrap modal contents with `focus-trap-react` and manage native dialog open states deterministically.
## 2024-06-15 | [Architectural Audit] | Insight: Silent async states | Protocol: Apply `aria-live="polite"`, `aria-busy="true"`, and `role="status"` on Loading, Error, and Empty states of network-dependent components.
## 2024-06-15 | [Architectural Audit] | Insight: Anonymous default exports | Protocol: Assign objects to a named variable before `export default` to adhere to modern ESLint rules and maintain strict type-safety standards.
## 2024-07-17 | [Architectural Audit] | Insight: Native `confirm()` blocks the event loop and disrupts A11y flow | Protocol: Replaced with DaisyUI's accessible `ConfirmModal` component to implement robust `focus-trap-react` and ARIA attributes.
## 2024-07-17 | [Architectural Audit] | Insight: Missing deterministic async states (Loading, Success, Error) during network events lock UI entirely | Protocol: Implemented item-specific loading IDs (e.g. `deletingId`, `launchingId`) alongside deterministic `aria-live="polite"` regions for notification messages to enable fluid UI interactions and announce changes to screen readers.
## 2024-07-17 | [Architectural Audit] | Insight: Widespread usage of `any` types and silent catch blocks compromise type safety | Protocol: Eradicated `any` types in favor of structured `Record<string, unknown>` interfaces and enforced strict `e instanceof Error` type guarding in all exception handlers.
