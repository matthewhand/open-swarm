## 2025-05-18 | [Architectural Audit] | Insight: Native buttons with a bare `loading` class drop structural integrity, breaking DaisyUI 5's new rendering requirement and reducing screen-reader utility. | Protocol: Explicitly render a `.loading.loading-spinner` child with `role="status"` instead of relying on broken CSS classes, ensuring full A11y and CI compliance.
## 2025-05-18 | [Architectural Audit] | Insight: The testing environment strictly enforces structural queries like `.getByRole()` over `.querySelector()`. Testing library methods must not poll against `.container`. | Protocol: Replace `container.querySelector` with `screen.getByRole` for general elements and add required `// eslint-disable-next-line` overrides for structurally inaccessible `.loading-spinner` queries to fix `testing-library/no-container` and `testing-library/no-node-access` ESLint errors.
## 2025-05-18 | [Architectural Audit] | Insight: Native `confirm()` dialogs block the main thread, provide terrible accessibility, break styling conventions, and induce unexpected JS runtime behavior. | Protocol: Import `ConfirmModal`, define a state variable `teamToDelete` to track which team is being removed, and modify `handleDelete` to open the modal and defer the deletion to a confirmation callback, replacing the `confirm()` usage.
