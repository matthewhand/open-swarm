## 2024-07-30 | [Architectural Audit] | Insight: Brittle global DOM traversal in virtual-DOM components. | Protocol: Refactored `Tabs.tsx` to utilize a React `useRef` Map instead of `document.getElementById`, ensuring keyboard focus management respects the React component lifecycle and avoids potential global namespace collisions.
## 2024-07-30 | [Architectural Audit] | Insight: Thread-blocking native confirm dialogs disrupt SPA flow and create accessibility barriers. | Protocol: Replaced `confirm()` in `TeamsPage.tsx` with the semantic DaisyUI `ConfirmModal`, implementing robust local state (`teamToDelete`) to manage asynchronous destructive actions deterministically.
## 2024-07-30 | [Architectural Audit] | Insight: Conflicting ARIA patterns on dynamic state indicators (loading spinners). | Protocol: Refactored `Button.tsx` to use an explicit `role="status"` and `aria-label` on the loading indicator, removing visually hidden `sr-only` text and `aria-hidden` attributes that confuse assistive technologies. Adjusted test queries in `Button.test.tsx` and `A11y.test.tsx` to strictly use semantic `getByRole` methods, satisfying `testing-library` lint rules without overrides.
