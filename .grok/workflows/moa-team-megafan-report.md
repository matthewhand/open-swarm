# MoA team megafan report

## Audit
- agents ok: 64
- agents fail: 0
- raw findings: 307
- verified: 80
- confirmed: 43

## Confirmed findings
[{"evidence":"parse_team_tasks: `return tasks or None` at /home/matthewh/open-swarm/src/swarm/core/moa/team.py:145 and :177; empty raw → None at :118-119. Tests: parse_team_tasks([]) is None; parse_team_tasks('|||') is None (tests/core/test_moa_team.py). run_moa_then_team: `if specialist_tasks is None: specialist_tasks = [TeamTask(purpose='implementer', ..., output_path='decision.md')]` at team.py:460-467; test_default_specialist_when_tasks_none asserts decision.md. Blueprint: tasks = self._parse_tasks(); run_moa_agents_orchestrator(..., specialist_tasks=tasks) with no empty guard (blueprint_moa_orchestrator.py:119-124). Counter: swarm_cli.py:415-423 rejects empty parse with Exit(2); test_moa_command asserts no decision.md for '|||'.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Distinguish missing tasks (None → default) from empty list ([] → no specialists). Stop mapping [] / all-invalid input to None, or document and test that [] is an intentional alias for the default implementer.","issue":"Empty or fully-skipped task lists collapse to None, which run_moa_then_team treats as “schedule default implementer” (writes decision.md). There is no parse result that means “explicitly zero specialists.”","reason":"Shipped parse_team_tasks returns `tasks or None` (team.py:145,177), so [] / '|||' / all-skipped segments become None; run_moa_then_team (team.py:460-467) treats None as default implementer writing decision.md. parse never returns [] so it cannot encode explicit zero specialists (only a direct specialist_tasks=[] call would). Blueprint still forwards parse None unchanged. Claim overstates severity and wrongly implies CLI still passes empty through—CLI exits 2 when not tasks.","severity":"high"},{"evidence":"orchestrator.py L257-271: await asyncio.wait_for(coro=backend.consult(...), timeout=per_participant_timeout) except TimeoutError -> ParticipantOpinion only, no process cleanup. backends.py Grok L207-234: create_subprocess_exec then wait_for(communicate, default_timeout); proc.kill()+wait only in except TimeoutError; no CancelledError/finally. backends.py Acpx L323-337: create_subprocess_exec then bare await proc.communicate(); timeout only via CLI --timeout in build_command L297-299, no Python kill. Nested case: outer cancel before Grok default_timeout=180 (or Acpx 300) skips Grok kill path. Call sites: cli.py L138-142 and blueprint_moa.py L123-126 construct MoAOrchestrator without per_participant_timeout; only test_moa_leftovers.py L40 sets it.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/orchestrator.py","fix_hint":"Document that per_participant_timeout should be >= backend timeout, or add a backend cancel/cleanup hook (try/finally kill) and/or shield communicate with explicit kill on orchestrator timeout.","issue":"Orchestrator wait_for timeout cancels backend.consult without process cleanup. Live backends only kill subprocesses on their own TimeoutError (e.g. Grok after communicate timeout); CancelledError from outer wait_for can leave orphaned grok/acpx processes when per_participant_timeout is shorter than backend default_timeout.","reason":"Shipped code matches the claim: MoAOrchestrator wraps backend.consult in asyncio.wait_for and on TimeoutError only builds a failed ParticipantOpinion; cancel of the consult task does not kill subprocesses. GrokParticipantBackend only proc.kill()s in except asyncio.TimeoutError around communicate (CancelledError bypasses that). AcpxParticipantBackend awaits bare proc.communicate() with no kill/finally. If per_participant_timeout is set and fires before the backend's own timeout path, outer cancel can leave grok/acpx children running. Severity is medium rather than high because CLI and blueprint never pass per_participant_timeout (default None), so the outer wait_for path is latent unless a caller opts in; impact is process/resource leak, not a privilege or integrity bug.","severity":"high"},{"evidence":"plain object: src/swarm/core/moa/cli.py L169-178 `\"determination\": ({ \"answer\": ..., \"rationale\": ..., \"participant_names\": ..., \"analysis\": ... } if result.determination else None)`. team str: src/swarm/core/moa/team.py L55 `determination: str`; L445 `det = (moa_payload.get(\"determination\") or {}).get(\"answer\") or \"\"`; L488-489 `MoATeamResult(determination=det, moa_payload=moa_payload, ...)`; L181-187 `team_result_to_payload` sets `\"determination\": result.determination` and `\"moa\": result.moa_payload` (nested object from consult_moa→run_moa_cli). CLI branch: src/swarm/core/swarm_cli.py L407-440 team→team_result_to_payload vs L456-471 else→run_moa_cli; L485-488 both `json.dumps(payload)`. Tests: tests/cli/test_moa_command.py L115 plain `assert data[\"determination\"] is not None` (no type); L157 team `data[\"determination\"].lower()`; L529-530 `isinstance(data[\"determination\"], str)`. format_team_text L214-216 dual-handles dict|str for human output only.","file":"/home/matthewh/open-swarm/src/swarm/core/swarm_cli.py","fix_hint":"Normalize team JSON to the same determination object as run_moa_cli (keep a string field like determination_text if needed), or always document/require branching on mode/specialists and add a schema version key.","issue":"`moa --json` uses incompatible top-level `determination` shapes: plain MoA emits an object (`answer`/`rationale`/…); `--team` emits a bare string (from `team_result_to_payload`). Nested `moa.determination` is still an object. Scripts that do `data[\"determination\"][\"answer\"]` break when `--team` is added.","reason":"Same CLI command family (`swarm-cli moa --json`) emits incompatible top-level `determination` types depending on `--team`. Plain MoA builds an object with answer/rationale/participant_names/analysis; team mode serializes MoATeamResult.determination which is a bare str (answer text only), while the full object remains nested under `moa.determination`. Access patterns like data[\"determination\"][\"answer\"] TypeError when --team is added. Tests encode both shapes rather than unifying them.","severity":"high"},{"evidence":"docs/SWARM_WORKFLOWS.md:6-8 top table: '**A. Orchestrated consensus (MoA)**' vs '**B. Persona / agent-as-tool swarm**' with primary name 'openai-agents team / persona swarm'. :32-37 diagram 'B. Persona swarm' with 'Coordinator Agent (openai-agents)'. :68 '## B — Persona / agent-as-tool swarm (openai-agents)'. :213 first-class pack link: 'Simple consensus vs consensus→team | No | moa-consensus-vs-team'. :256 prove table: '**A team** | `run_moa_then_team` / `swarm-cli moa --team` — … no openai-agents' and '**B** | Real `agents.Agent` personas…'. docs/examples/moa-consensus-vs-team/README.md:3 '**No openai-agents required.**'; :8-9 table '**A. Consensus only** | `run_moa_consensus`' vs '**B. Consensus → team** | `run_moa_then_team`'; :34 diagram 'Path A: stop here' vs 'Path B: drive a team'; :15 Related link to SWARM_WORKFLOWS.md; :201 'openai-agents import | **not used** | **not used**'. CHANGELOG.md:264 dual taxonomy '**A** MoA consensus … vs **B** openai-agents persona swarm'.","file":"/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/README.md","fix_hint":"Rename example paths to 'consensus_only' / 'consensus_then_team' (or Path A1/A2 under MoA), never reuse global B for non-persona team.","issue":"Path A/B labels collide with the global SWARM_WORKFLOWS dual taxonomy. Global B = openai-agents persona swarm; this first-class pack (linked from SWARM_WORKFLOWS) redefines Path B as pure consensus→team with no openai-agents.","reason":"Independent read confirms a real cross-doc A/B nomenclature collision: SWARM_WORKFLOWS defines global B as the openai-agents persona swarm, while the first-class pack it links (moa-consensus-vs-team) reuses Path B for pure run_moa_then_team with no openai-agents. SWARM_WORKFLOWS itself labels that same API as 'A team', proving the pack's B is not global B. Impact is reader/taxonomy confusion (docs only), not runtime failure—so severity is medium, not high.","severity":"high"},{"evidence":"README L79: 'This is **model A then model B** … then agent-as-tool specialists that *may* write.' vs agents_orchestrator.py L218-253: docstring 'Runs the pure run_moa_then_team path' / 'Real openai-agents objects are **not** constructed here'; body only `team = await run_moa_then_team(...)` + SCRIPTED_ORCHESTRATOR_ROSTER; no Runner.run in file. as_tool only in build_moa_orchestrator_agents L185-195 (unused by run). team.py L373-384 'scripted R/W team — no openai-agents'; _run_specialist L310-348 template writes. Blueprint L121 also calls run_moa_agents_orchestrator only.","file":"/home/matthewh/open-swarm/docs/examples/moa-orchestrator/README.md","fix_hint":"Describe as A (MoA) then scripted TeamTask specialists; say agent objects are optional/inspection-only until a real Runner path exists.","issue":"Claims the orchestrator path is 'model A then model B' with agent-as-tool specialists, but the executed body is pure scripted team (`run_moa_then_team`), not persona-swarm B or live agent-as-tool tasking.","reason":"Canonical moa-orchestrator README L79 states the path is model A then model B with agent-as-tool specialists, but the documented/shipped execution entrypoint run_moa_agents_orchestrator only awaits pure scripted run_moa_then_team (hardcoded WorkspaceTools writers), never builds Agents, never calls Runner.run, and never exercises as_tool wiring from build_moa_orchestrator_agents.","severity":"high"},{"evidence":"docs/examples/moa-consensus-vs-team/README.md L42: `| Panel is read-only | \\`consult_moa(..., act=False)\\` + \\`approve-reads\\` |`. src/swarm/core/moa/tools.py L15-24: `async def consult_moa(question, participants=None, *, backend='fake', fake_responses=None, cwd=None, permission=..., timeout=300.0)` — no `act`. L42-51: `return await run_moa_cli(..., act=False)` hardcodes act only on the lower-level CLI runner. Call sites (team.py, agents_orchestrator.py, tests) never pass `act` to `consult_moa`.","file":"/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/README.md","fix_hint":"Rewrite as “consult_moa always no-act (hardcoded); panel permission approve-reads” or show the real signature + note that act is not exposed.","issue":"Enforcement table documents `consult_moa(..., act=False)` as if `act` were a public parameter. Real `consult_moa` has no `act` kwarg; it hardcodes `act=False` only when calling `run_moa_cli`. Copy-paste of the documented call form raises TypeError.","reason":"Enforcement table at README L42 documents `consult_moa(..., act=False)` as if `act` were a public keyword argument. The real `consult_moa` signature in tools.py has only question, participants, backend, fake_responses, cwd, permission, and timeout — no `act`. Internally it hardcodes `act=False` when calling `run_moa_cli`. Passing `act=False` to `consult_moa` is therefore an unexpected keyword and raises TypeError; the docs are copy-paste-wrong (correct form is already shown without `act` in docs/MOA.md).","severity":"high"},{"evidence":"README L1: \"Mixture of Agents + openai-agents orchestrator\"; L17–20 contrasts sibling pure run_moa_then_team with this page's \"openai-agents orchestrator surface (`moa_orchestrator` / `run_moa_agents_orchestrator`)\"; L244 \"Step 4 — openai-agents orchestrator + specialists\" imports/calls only run_moa_agents_orchestrator (L249–273); sequence diagram L139 API->>Coord: run_moa_agents_orchestrator. agents_orchestrator.py L204–253: docstring \"Real openai-agents objects are **not** constructed here\"; body `team = await run_moa_then_team(...)` then `agents=dict(SCRIPTED_ORCHESTRATOR_ROSTER)` (string roster L55–61). test_moa_agents_orchestrator.py L59–62 asserts lightweight name roster. blueprint_moa_orchestrator.py L121–129 only calls run_moa_agents_orchestrator (no Agent/Runner).","file":"/home/matthewh/open-swarm/docs/examples/moa-orchestrator/README.md","fix_hint":"Retitle/reframe Step 4 as agents-mode result shape over pure team runner; point live Agent/Runner wiring at build_moa_orchestrator_agents only; align with sibling moa-consensus-vs-team wording.","issue":"README markets run_moa_agents_orchestrator as the openai-agents orchestrator execution path, but post pure-team path the function only wraps run_moa_then_team and never constructs or runs openai-agents Agents.","reason":"Independent read of shipped docs and code confirms a real high-severity docs/API mismatch: docs/examples/moa-orchestrator/README.md titles and Step 4 present run_moa_agents_orchestrator as the openai-agents orchestrator execution surface (and contrasts it with the pure team path), but agents_orchestrator.run_moa_agents_orchestrator only awaits run_moa_then_team, returns agents=dict(SCRIPTED_ORCHESTRATOR_ROSTER) string names, and its own docstring states real openai-agents objects are not constructed. The blueprint and unit test use the same scripted path. build_moa_orchestrator_agents can construct Agents, but Step 4 never calls it and run_moa_agents_orchestrator never invokes Runner.","severity":"high"},{"evidence":"README.md L39-49: box \"Orchestrator (openai-agents coordinator)\" with consult_moa + task_*; L54-68 mermaid Coord[openai-agents coordinator]; L79 \"agent-as-tool specialists\"; L127-156 sequence participant \"Agents coordinator\", API->>Coord: run_moa_agents_orchestrator, Coord->>Imp/Tst/Docs. agents_orchestrator.py L218-230 docstring: scripted path reuses run_moa_then_team; \"Real openai-agents objects are not constructed here\"; L232-253 body only awaits run_moa_then_team and sets agents=SCRIPTED_ORCHESTRATOR_ROSTER. team.py L1-11, L262-268 _run_specialist(WorkspaceTools), L373-451 run_moa_then_team loops _run_specialist (no Agent.as_tool). blueprint_moa_orchestrator.py L121-129 only calls run_moa_agents_orchestrator. as_tool exists only in build_moa_orchestrator_agents L185-195, unused by run_moa_agents_orchestrator.","file":"/home/matthewh/open-swarm/docs/examples/moa-orchestrator/README.md","fix_hint":"Redraw §2.2 like sibling path B (run_moa_then_team / WorkspaceTools); optionally add a separate diagram for build_moa_orchestrator_agents + optional Runner.","issue":"§1 architecture + §2.2 sequence diagram depict an Agents coordinator that consults MoA then tasks implementer/tester/docs as agent tools; the documented/CI path does not run that loop.","reason":"Independent read confirms a real architecture/docs mismatch: moa-orchestrator README §1 ascii/mermaid and §2.2 sequence present an openai-agents Agents coordinator that consults MoA then tasks implementer/tester/docs (agent-as-tool style). The documented entrypoint run_moa_agents_orchestrator and moa_orchestrator blueprint only await run_moa_then_team; specialists write via team._run_specialist + WorkspaceTools, with no Agent construction, Runner, or as_tool task loop. build_moa_orchestrator_agents can wire as_tool but is not what the walkthrough/CI path runs.","severity":"high"},{"evidence":"demo_moa_consensus_vs_team.py L131-133,L144-148: invariants a_writes_empty=a.writes==[], neither_panel_wrote=(not a.panel_wrote)and(not b.panel_wrote); ok=all(invariants); return 0 if ok. team.py L62-65: panel_wrote property always returns False. team.py L245-256: panel_writes only logged; return MoATeamResult(..., writes=[]). cli.py L188: real writes surface is payload['writes']=list(write_surface.writes). trace_moa_champagne.py L57-61: non-tautological check (a.moa_payload.get('writes') or [])==[].","file":"/home/matthewh/open-swarm/scripts/demo_moa_consensus_vs_team.py","fix_hint":"Assert (a.moa_payload.get(\"writes\") or []) == [] and same for b.moa_payload; keep a.writes == [] and optionally treat panel_wrote as documentation-only.","issue":"Exit-gated write invariants are tautological: they read hard-coded MoATeamResult surfaces, not MoA panel payload writes, so a panel-write regression would still exit 0.","reason":"Confirmed: demo exit gates assert a.writes==[] and not *.panel_wrote, but MoATeamResult.panel_wrote always returns False and run_moa_consensus always returns writes=[]; neither inspects moa_payload writes. A non-empty panel/act write surface on the payload would not flip those invariants, so the demo can still exit 0. Champagne tracer correctly checks (a.moa_payload.get('writes') or []) == [] for path A.","severity":"high"},{"evidence":"src/swarm/core/moa/team.py run_moa_then_team L411–451: moa_payload = await consult_moa(...); det = (moa_payload.get('determination') or {}).get('answer') or ''; then if record_determination: tools.write_file('moa_determination.md', ...); then for task in specialist_tasks: _run_specialist(..., determination=det) with no check of moa_payload['opinions'][].ok or determination.analysis.ok_count. _run_specialist implementer/tester/docs L310–347 always write_file with det embedded. Soft-fail source: src/swarm/core/moa/orchestrator.py default_synthesize L65–74: if not ok: return Determination(answer='No usable participant opinions.', analysis={'ok_count': 0}). Payload exposes both: src/swarm/core/moa/cli.py L150–175 opinions[].ok + determination.answer/analysis. consult_moa (tools.py L42–50) returns that payload with act=False.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"After consult_moa, if no usable opinions (ok_count==0 or all opinions ok=False), raise a dedicated error or return MoATeamResult early with mode/flag and specialist_results=[], writes limited or empty; do not schedule specialists.","issue":"Soft consult_moa / panel failures do not stop consensus_then_team: after consult_moa returns, team always writes moa_determination.md and runs R/W specialists, even when every seat failed (ok_count=0 / answer is the degradation sentinel).","reason":"run_moa_then_team never gates on panel success: after consult_moa it always extracts determination.answer, optionally writes moa_determination.md, and runs every specialist with that text. Soft total panel failure is synthesized in default_synthesize as answer='No usable participant opinions.' with analysis.ok_count=0 (and opinions[].ok=false in the payload), but team.py ignores both. Specialists still write decision/test/docs files embedding the sentinel, so a fully failed consult does not stop consensus_then_team R/W side effects.","severity":"high"},{"evidence":"tests/cli/test_moa_command.py:149 and :527 assert data[\"mode\"] == \"consensus_then_team\" with panel_wrote/writes; test_swarm_cli_moa_subprocess_fake (94-116) and integration test_workflow_a_swarm_cli_moa_entry only check opinions/determination/permission_mode. grep tests/ finds consensus_only mode asserts only in tests/core/test_moa_team.py. swarm_cli.py:407-471 routes --team via team_result_to_payload else run_moa_cli; run_moa_cli payload (src/swarm/core/moa/cli.py:163-189) has no \"mode\" key.","file":"/home/matthewh/open-swarm/tests/cli/test_moa_command.py","fix_hint":"Add test_swarm_cli_moa_consensus_only_mode mirroring team tests: swarm-cli moa ... --json without --team; assert mode, empty writes/specialists, panel_wrote is False.","issue":"No Typer subprocess test asserts pure-path consensus_only when --team is omitted. Team path has mode/writes/panel_wrote coverage; path A has no counterpart.","reason":"Independent read confirms no Typer subprocess test asserts mode==\"consensus_only\" when --team is omitted; team CLI tests assert consensus_then_team plus writes/panel_wrote. Claim line numbers are stale (team mode asserts at 149 and 527; non-team fake subprocess is 94-116). Severity is not high: pure-path run_moa_cli JSON never includes a mode field (cli.py 163-189), so the gap is coverage/schema asymmetry, and consensus_only is already asserted in tests/core/test_moa_team.py.","severity":"high"},{"evidence":"src/swarm/core/swarm_cli.py:407–471 if team: run_moa_then_team + team_result_to_payload(...); else: run_moa_cli(...) — no run_moa_consensus import/call (rg empty on swarm_cli.py). src/swarm/core/moa/cli.py:163–189 payload keys question/participants/backend/permission/opinions/determination/act/writes only; no \"mode\". src/swarm/core/moa/team.py:241–286 run_moa_consensus returns MoATeamResult(mode=\"consensus_only\"); 181–202 team_result_to_payload sets \"mode\": result.mode. docs/examples/moa-consensus-vs-team/README.md:8 Path A API = run_moa_consensus / swarm-cli moa; 123 assert result.mode == \"consensus_only\"; 130–138 CLI equivalent is swarm-cli moa without --team; 198 Path A result.mode = consensus_only. tests/cli/test_moa_command.py:93–115 non-team CLI asserts opinions/determination not mode; 148 --team asserts data[\"mode\"] == \"consensus_then_team\" only.","file":"/home/matthewh/open-swarm/src/swarm/core/swarm_cli.py","fix_hint":"For act=False non-team, call run_moa_consensus + team_result_to_payload (keep run_moa_cli for --act); then add the missing CLI consensus_only JSON test.","issue":"Non-team CLI never calls run_moa_consensus/team_result_to_payload, so a consensus_only mode contract cannot hold and is untestable at CLI today. Docs present swarm-cli moa (no --team) as Path A.","reason":"Shipped swarm_cli moa branches: --team calls run_moa_then_team + team_result_to_payload (mode from MoATeamResult); without --team it only awaits run_moa_cli. run_moa_consensus is never referenced in swarm_cli.py. run_moa_cli's payload dict has no mode key. Docs Path A tables and the explicit CLI equivalent under run_moa_consensus still map Path A to swarm-cli moa without --team, while the consensus_only mode contract is set only in run_moa_consensus → MoATeamResult(mode=\"consensus_only\") and serialized by team_result_to_payload. CLI tests assert mode only on the --team path (consensus_then_team), so the consensus_only mode contract is not exercisable via non-team CLI today. (Claimed swarm_cli line numbers ~357–402 are stale; logic is at 407–471. Claimed README ~130–138 is docs/examples/moa-consensus-vs-team/README.md, not top-level README.md.)","severity":"high"},{"evidence":"_default_fakes (team.py:68-78) returns only keys analyst/critic. run_moa_consensus (227-230) and run_moa_then_team (397-400): seats=list(moa_participants or [\"analyst\",\"critic\"]); if moa_backend==\"fake\" and not fakes: fakes=_default_fakes(question); then consult_moa(question, seats, fake_responses=fakes). FakeParticipantBackend.consult (backends.py:118-125): text=self.responses.get(agent); if text is None → ok=False error=f\"unknown participant {agent!r}\". consult_moa itself (tools.py:31-35) would stub all names when fake_responses empty, but team pre-fills analyst/critic-only fakes so that fallback never applies. CLI contrast swarm_cli.py:402-405 maps default_texts onto each name in names. agents_orchestrator.py:104-105 reuses same _default_fakes pattern.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Generate default fakes for each seat name (like CLI/consult_moa stubs), or refuse fake backend when provided seats are not covered by responses.","issue":"Team fake defaults are hard-keyed to analyst/critic and do not follow moa_participants; FakeParticipantBackend then fails every custom seat with unknown participant.","reason":"Shipped team.py hard-keys fake defaults to analyst/critic while consulting arbitrary moa_participants; FakeParticipantBackend then returns ok=False unknown participant for every unmatched seat. Confirmed end-to-end vs CLI which maps defaults onto each name.","severity":"high"},{"evidence":"Shipped parse_team_tasks in /home/matthewh/open-swarm/src/swarm/core/moa/team.py lines 111–128 always splits on the last '@' with no path heuristic:\n\n```\noutput_path: str | None = None\nif \"@\" in part:\n    part, output_path = part.rsplit(\"@\", 1)\n    output_path = output_path.strip() or None\n...\nif output_path is None:\n    output_path = _DEFAULT_OUTPUT_PATHS.get(purpose.lower())\n```\n\nDocstring documents `purpose:instruction@rel/path` (lines 95–99). Trace of claim example `implementer:notify a@b.com`:\n1) rsplit('@',1) → part=`implementer:notify a`, output_path=`b.com`\n2) split(':',1) → purpose=`implementer`, instr=`notify a`\n3) output_path stays `b.com` (not replaced by default)\n→ TeamTask(instruction='notify a', output_path='b.com').\n\nSame for `implementer:ping user@example.com` → instruction `ping user`, output_path `example.com` (not decision.md).\n\nDict/list form (lines 136–145) is unaffected. No `_looks_like_output_path` / `_safe_output_path` exists under src/ (grep empty). Caveat on claim's \"No test\": tests/core/test_moa_team.py already expects emails preserved (e.g. lines 497–508: `implementer:ping user@example.com` → instruction `ping user@example.com`, output_path `decision.md`; also 287–291, 520–526, 1003–1008) and comments name `_looks_like_output_path`—so missing coverage is false/outdated; the implementation simply still has the naive rsplit, so those tests document the bug rather than disprove it.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Require an explicit path marker (e.g. only split '@' when path-like, or use a safer separator), or document that '@' is reserved and reject ambiguous segments.","issue":"String mini-language always treats the last '@' as an output-path delimiter, so instructions containing '@' (emails, handles) mis-parse into path + truncated instruction.","reason":"Independent read of team.py confirms unconditional last-'@' split; email/handle instructions are truncated and the suffix becomes output_path. Medium is appropriate for a CLI mini-language footgun with a safe dict API alternative. Claim's 'no test' detail is wrong (tests expect the fixed behavior), but the mis-parse bug itself is real in shipped code.","severity":"medium"},{"evidence":"src/swarm/core/moa/team.py parse_team_tasks list branch (lines 131–150):\n  if isinstance(raw, list):\n      tasks = []\n      for item in raw:\n          if isinstance(item, TeamTask):\n              tasks.append(item)\n          elif isinstance(item, dict):\n              ... tasks.append(TeamTask(...))\n          elif isinstance(item, str):\n              nested = parse_team_tasks(item)\n              if nested:\n                  tasks.extend(nested)\n      return tasks or None\nNo else/warning for other types; e.g. [TeamTask(...), None, 42, [\"nested\"]] yields only the TeamTask entries. Docstring (101) only documents dicts/strings. tests/core/test_moa_team.py: test_parse_team_tasks_list_of_dicts (336–355) and test_parse_team_tasks_nested_string_in_list (370–383) only use valid item types; test_parse_team_tasks_never_raises_on_randomish_input (442–447) only asserts no throw / TeamTask list, not that invalid list items are rejected or warned.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Raise or log on unexpected list element types; optionally fail closed if any item is invalid.","issue":"List form silently ignores non-str/dict/TeamTask items (None, int, nested list, etc.), so partial/corrupt task lists parse without warning.","reason":"List parsing only handles TeamTask/dict/str; any other list element is dropped with no log, raise, or filter signal, so mixed valid+invalid lists return a partial task list. That matches the claim and is a real lenient-API footgun (not mere style), though impact is low: CLI usually passes strings, and top-level non-list junk already returns None.","severity":"low"},{"evidence":"src/swarm/core/moa/team.py:268 purpose = task.purpose.lower().strip(); 270–277 unknown branch returns PersonaResult(persona=task.purpose, ..., ok=False); 356–360 success returns PersonaResult(persona=purpose, ..., ok=True); 365 exception also persona=purpose. tests/core/test_moa_team.py:610 TeamTask(\"wizard\", ...); 619–620 by = {s.persona: s for s in result.specialist_results}; assert by[\"wizard\"].ok is False — locks original-case key for unknown path while known personas are lowercased (e.g. assert sets with \"implementer\").","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Always set persona to the normalized lowercase purpose (keep original in output message if needed).","issue":"Persona identity in MoATeamResult.specialist_results is inconsistent: successful specialists use lowercased purpose; unknown purposes keep the original Task.purpose casing.","reason":"Confirmed identity inconsistency: unknown specialists set PersonaResult.persona to raw task.purpose, while known specialists (success and exception paths) set persona to purpose after lower().strip(). Callers indexing specialist_results by persona (as the unit test does) therefore see original casing only on the unknown path, and mixed-case known purposes are normalized. Real but low impact—contract quirk, not a functional failure of specialist execution.","severity":"low"},{"evidence":"src/swarm/core/moa/team.py MoATeamResult: class docstring is only 'Outcome of pure MoA consensus or consensus-then-team.'; field `final: str = \"\"` has no field doc. run_moa_then_team: `final = specialist_results[-1].output if specialist_results else det` then `return MoATeamResult(..., final=final)`. run_moa_consensus sets `final=det`. team_result_to_payload: `\"final_preview\": (result.final or \"\")[:800]`. docs/examples/.../05-demo-consensus-then-team.json: multi-specialist writes + final_preview is the docs ADR body (same as last specialist output_preview). tests/core/test_moa_team.py: multi-task payload tests assert specialist count/personas/writes and isinstance(final_preview, str) / len<=800 only—no assert that final equals last specialist or is not an aggregate.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Document on MoATeamResult, or set final to determination (or a joined multi-specialist summary) for multi-task runs.","issue":"MoATeamResult.final is only the last specialist’s output (or determination if none); multi-specialist runs do not aggregate. Class docstring does not document this, so final/final_preview can look like a full team summary when it is not.","reason":"Independent read of team.py confirms MoATeamResult.final is assigned only as the last specialist's output (or determination when there are none); multi-specialist runs do not join or synthesize specialist outputs into final. The class documents only a generic outcome and never states this last-specialist semantics. team_result_to_payload maps that field to final_preview (truncated). Example multi-specialist JSON shows final_preview equal to the last specialist's body (docs), not a team aggregate. Tests only check final_preview type/length, not multi-specialist final semantics. Low severity: correct data still lives in specialist_results/writes; risk is misleading preview naming/docs, not lost team work.","severity":"low"},{"evidence":"team.py:429 task_names=[]; 434 logs task_names or [\"implementer(default)\"] so [] looks like default; 460 only if specialist_tasks is None installs implementer; 470 for task in specialist_tasks is a no-op for []; 480 final=det when specialist_results empty; 490 mode still consensus_then_team. parse_team_tasks returns tasks or None (145/177) so CLI empty→None avoids this; explicit [] does not.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Treat not specialist_tasks the same as None for default implementer, or log tasks=[] / tasks_n=0 without inventing implementer(default).","issue":"specialist_tasks=[] is not treated like None: no default implementer runs, but logs claim implementer(default) and tasks_n=1.","reason":"Independently confirmed: specialist_tasks=[] skips the None-only default implementer (team.py:460-470), runs zero specialists, but start logging still prints implementer(default) via task_names or [...] (team.py:429-434). Claimed tasks_n=1/line numbers ~559-604 are outdated; semantic bug remains.","severity":"medium"},{"evidence":"Confirmed in /home/matthewh/open-swarm/src/swarm/core/moa/team.py. (1) Map L82-87: _DEFAULT_OUTPUT_PATHS = {\"implementer\": \"decision.md\", \"tester\": \"test_notes.md\", \"docs\": \"docs/ADR.md\", \"researcher\": \"research_notes.md\"}; used by parse_team_tasks L121-122 (string form) and L142-143 (dict form). (2) _run_specialist does NOT read the map—hardcoded fallbacks: L301 path = task.output_path or \"research_notes.md\"; L311 or \"decision.md\"; L329 or \"test_notes.md\"; L340 or \"docs/ADR.md\". (3) run path when specialist_tasks is None L433-439: TeamTask(purpose=\"implementer\", instruction=\"Apply the MoA determination to decision.md\", output_path=\"decision.md\"). Same literal \"decision.md\" therefore lives in map, specialist runner, and default TeamTask; tester/docs/researcher map vs runner hardcodes match the claimed parallel pattern. Drift is path-dependent (parse uses map; None-task default and None output_path use other sites).","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Single helper e.g. default_output_path(purpose) used by parse, default task construction, and _run_specialist.","issue":"Default implementer output path is triplicated (map, parse fallback, run_moa_then_team default TeamTask, and _run_specialist or \"decision.md\"), inviting drift if one site changes.","reason":"Independent read shows three separate sources of truth for implementer default output (and map vs _run_specialist for all four purposes). Currently consistent, so no live bug, but changing one site without the others would silently diverge defaults across parse vs default TeamTask vs output_path=None specialists—real maintainability/drift risk, not mere formatting nit.","severity":"medium"},{"evidence":"In /home/matthewh/open-swarm/src/swarm/core/moa/team.py _run_specialist implementer branch: L290 det = determination; L316-318 if (tools.root / \"moa_determination.md\").exists(): tools.read_file(\"moa_determination.md\"); trace.append(...) — return value not assigned; L319-324 body uses f\"## MoA consensus\\n{det}\\n\" (in-memory only). Contrast L313-315 notes = tools.read_file(\"notes.txt\") which is used in body. Claimed ~427-429 line refs are wrong (that region writes moa_determination.md); the unused-read pattern is at 316-318.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Use the read text (or det fallback) in the Decision body, or drop the dead read if only det is authoritative.","issue":"Implementer reads moa_determination.md for trace/reads accounting but discards content; body uses in-memory determination only.","reason":"Independent read confirms the implementer performs a side-effect-only read of moa_determination.md (trace/tools.reads accounting) and discards file content; decision body is filled from the determination parameter, not the file. Behavior is real and low-severity (scripted specialist; content should match orchestrator-written det barring external edits).","severity":"low"},{"evidence":"src/swarm/core/moa/team.py:90-129 parse_team_tasks string branch: if \"@\" in part: part, output_path = part.rsplit(\"@\", 1); output_path = output_path.strip() or None — no email/path check; default path only when output_path is None (L121-122). No _looks_like_output_path/_safe_output_path anywhere under src/swarm/core/moa/ (only test comments). Writes use task.output_path (e.g. L311 implementer, L329 tester, L340 docs). tests/core/test_moa_team.py:497-508 parametrize expects implementer:ping user@example.com → instruction keeps email, output_path decision.md; docs:…@corp.io → full instruction, docs/ADR.md; L491-493 expect bare @suffix without path-like shape to stay in instruction — contradicts L112-114 shipped logic.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Restore a path-shape heuristic (or require explicit @path only when candidate has / or a known extension and no spaces-as-email) so domain-like suffixes do not become write targets for tester/docs.","issue":"String task grammar always treats the last '@' as output_path with no path-shape/email check. Tester/docs CLI tasks like 'docs:see alice@corp.io and bob@corp.io' or 'tester:ping user@example.com' silently retarget writes to 'corp.io' / 'example.com' inside the workspace instead of defaults (docs/ADR.md, test_notes.md).","reason":"Shipped parse_team_tasks unconditionally rsplit()s on the last '@' and assigns the right-hand side as output_path with no path-shape or email heuristic. Helpers named in tests (_looks_like_output_path, _safe_output_path) do not exist under src/swarm/core/moa/. Concrete examples: 'implementer:ping user@example.com' becomes instruction='ping user' and output_path='example.com' (not decision.md); 'docs:see alice@corp.io and bob@corp.io' becomes instruction='see alice@corp.io and bob' and output_path='corp.io' (not docs/ADR.md). Specialists then write via task.output_path, so CLI/docs/tester task strings containing emails silently retarget workspace writes. Tests in test_moa_team.py encode the intended safer grammar but that behavior is not implemented in team.py. Claim misnames the function as _parse_task_segment and overstates an email-ambiguity docstring (current docstring only describes purpose:instruction@rel/path); the defect itself is independently confirmed.","severity":"medium"},{"evidence":"In /home/matthewh/open-swarm/src/swarm/core/moa/team.py _run_specialist: tester body uses det[:1200] (L331); docs body embeds full question and full det with no slice — f\"## Context\\n{question}\\n\\n## Decision\\n{det}\\n\\n\" (L341-345); body is written via tools.write_file and appended to out_parts (L346-348), then returned as PersonaResult.output = \"\\n\".join(out_parts) with no cap (L355-360). implementer also embeds full det (L319-324). researcher caps at det[:1500] (L304). run_moa_then_team sets final = specialist_results[-1].output (L453), so if docs is last, result.final is the unbounded ADR body. record_determination also writes full det to moa_determination.md (L426-430). team_result_to_payload only truncates previews for JSON (output_preview[:500], final_preview[:800] at L168/L174), not workspace files or in-memory PersonaResult.output.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Cap det/question (and optionally instruction) for docs the same way as tester/researcher, or stream to disk without retaining the full body in PersonaResult.output.","issue":"Docs specialist embeds the full question and full MoA determination into the ADR body and PersonaResult.output with no size cap; tester only truncates determination to 1200 chars. Unbounded panel/backend output can inflate workspace artifacts, result.final (if docs is last), and memory.","reason":"Claim matches current code: asymmetric truncation is real (tester 1200, researcher 1500, docs/implementer uncapped). Full question+determination flow into workspace artifacts and PersonaResult.output; if docs is last specialist, result.final is uncapped. Not a correctness bug, but a genuine unbounded-artifact/memory inflation risk for large panel determinations—medium severity is appropriate.","severity":"medium"},{"evidence":"team.py:90-151 parse_team_tasks: rsplit('@')/split(':')/strip only; no _safe_output_path/_looks_like; grep src/swarm/core/moa empty for those names. test_moa_team.py:258-274 expects docs\\ADR.md→docs/ADR.md; 996-1021 docstring cites _safe_output_path/_looks_like_output_path and expects implementer:x@../../etc/passwd → instr keeps @.., output_path decision.md, and dict ../../etc/passwd|/etc/passwd → defaults (current code sets output_path to the escape / keeps dict path; bare dict returns None). Edge params 498-527 expect emails/parent-escapes not as paths. persona_swarm.py:72-101 WorkspaceTools._safe does \\→/ and escape reject; specialists write via tools.write_file(task.output_path).","file":"/home/matthewh/open-swarm/tests/core/test_moa_team.py","fix_hint":"Either implement the documented parse sanitization/normalization or rewrite tests to match fail-closed-at-write-only policy and drop references to missing helpers.","issue":"Tests document parse-time sanitization (_looks_like_output_path / _safe_output_path, backslash→POSIX normalize, email-not-path) that is absent from team.py. team.py intentionally does not sanitize at parse; only WorkspaceTools enforces the boundary. This is test/code drift on the security surface for tester/docs paths.","reason":"Independent read confirms tests assert parse-time path sanitization/heuristics (_safe_output_path, _looks_like_output_path, backslash→POSIX, email-not-path, dict escape fallback) that are absent from shipped parse_team_tasks; team.py only strips/splits and defaults, while WorkspaceTools._safe is the real boundary. Concrete mismatch would fail those tests against current team.py.","severity":"medium"},{"evidence":"team.py:329 path = task.output_path or \"test_notes.md\"; 340 path = task.output_path or \"docs/ADR.md\"; then tools.write_file(path, body); 362-370 except Exception → ok=False, output=str(e). parse_team_tasks (111-145) sets output_path from rsplit('@') / dict as-is; no _safe_output_path in src (grep only hits test comments). persona_swarm.py:90-100 raises ValueError \"path escapes workspace\". test_moa_team.py:997-1021 claims parse sanitization for dict/string escapes; 1083-1094 TeamTask(\"docs\",\"ADR\",\"/etc/passwd\") expects \"escapes workspace\" via run-layer fail-soft only.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Optional defense-in-depth: normalize/reject abs and '..' in _run_specialist before write, with a clear error distinct from WorkspaceTools.","issue":"_run_specialist for tester/docs does not pre-validate output_path; escape paths on hand-built TeamTask only fail inside WorkspaceTools. Correct fail-soft behavior exists, but parse and run layers are inconsistent with tests that claim parse sanitization for dict/string forms.","reason":"Shipped team.py assigns tester/docs (and other) output_path without pre-validation and only fail-softs when WorkspaceTools._safe raises; parse_team_tasks has no path sanitization despite tests documenting _safe_output_path/_looks_like_output_path. Escape blocking works for hand-built TeamTask via write_file, so impact is defense-in-depth / test-code drift, not an open write escape.","severity":"low"},{"evidence":"team.py _run_specialist tester (328-337): body uses f\"## Against determination\\n{det[:1200]}\\n\\n## Task\\n{task.instruction}\\n\\n\" then tools.write_file(path, body)—det truncated, instruction not. docs (339-347): f\"## Context\\n{question}\\n\\n## Decision\\n{det}\\n\\n## Task\\n{task.instruction}\\n\\n...\" with no truncation on det/question/instruction. Same file implementer uses full {det} and {task.instruction} (319-325); researcher uses det[:1500] but full instruction (302-307); record_determination writes full det (426-429). persona_swarm.py WorkspaceTools.write_file (110-117): _safe(path) then p.write_text(content, encoding=\"utf-8\")—no content filters. tests/core/test_moa_team.py test_specialist_very_long_instruction_writes_ok (1121-1132): long_instr = \"stress-\" + (\"y\" * 20_000); assert long_instr in decision.md body.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Optional bounds on instruction length and strip NULs before write_file for all specialists.","issue":"Tester/docs write untrusted determination and instruction into markdown with no content policy (null bytes, huge instructions, control chars). Traversal is not involved; residual content integrity / resource concern only.","reason":"Independent read of shipped code confirms tester/docs (and siblings) fully interpolate task.instruction into workspace markdown; tester only slices determination to 1200 chars while docs embeds full question + full det + full instruction. WorkspaceTools.write_file only path-sandboxes and does p.write_text(content) with no size, null-byte, or control-char policy. The cited 20k-instruction test further documents unbounded body write is intended/accepted. This is residual content-integrity/resource behavior inside an already-sandboxed workspace, not path traversal—severity low is appropriate.","severity":"low"},{"evidence":"src/swarm/core/moa/team.py:294-308 — if purpose == \"researcher\": listing = tools.list_files(\".\"); notes only if (tools.root / \"notes.txt\").exists() then tools.read_file(\"notes.txt\"); body = f\"# Research\\n\\n## Task\\n{task.instruction}\\n\\n...## Workspace\\n{listing}\\n\\n## Notes\\n{notes}\\n\"; tools.write_file(...). No parse of listing, no other read_file paths, instruction unused for control flow. src/swarm/core/persona_swarm.py:119-125 — list_files returns \"\\n\".join(sorted(x.name for x in p.iterdir())) only (top-level names).","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"For each non-dir name in listing (or a small allowlist), call read_file and include snippets; or at least also read moa_determination.md when present, and/or map instruction keywords to extra paths.","issue":"Researcher calls list_files('.') but never uses the listing to decide what to read; the only optional read is hard-coded notes.txt. task.instruction is embedded in the report only and does not change tool selection, so 'inventory/scan' tasks cannot discover other seed files or nested context.","reason":"In the scripted MoA team runner, researcher tool selection is fixed: always list_files('.'), optionally read only notes.txt if it exists, then write a report. listing is only string-interpolated into the body; task.instruction is only embedded under ## Task and never branches reads. list_files itself is non-recursive (iterdir names only), so nested files are neither listed for content reads nor selected dynamically. This is a real functional limitation for inventory/scan-style research tasks, not style-only.","severity":"medium"},{"evidence":"persona_swarm.py:119-125 list_files: return \"\\n\".join(sorted(x.name for x in p.iterdir())) after _safe(directory); no rglob/walk. team.py:294-305 researcher: listing = tools.list_files(\".\"); body includes f\"## Workspace\\n{listing}\\n\\n## Notes\\n{notes}\\n\" with notes only if (tools.root / \"notes.txt\").exists(). Seed writes use write_file(rel) so nested paths exist on disk but are invisible to this inventory.","file":"/home/matthewh/open-swarm/src/swarm/core/persona_swarm.py","fix_hint":"Add optional recursive listing (relative paths) or a depth-limited walk used by the researcher branch; keep basename-only as a default if CI output size matters.","issue":"WorkspaceTools.list_files is non-recursive and returns only basenames (sorted iterdir names). Researcher embeds that string as '## Workspace', so nested seed files (e.g. docs/*, subdirs) never appear in the inventory used by the specialist.","reason":"Independent read confirms WorkspaceTools.list_files is non-recursive (Path.iterdir) and returns only basenames (x.name). The scripted researcher specialist always calls list_files('.') and embeds that string under '## Workspace', so nested seed file paths (e.g. docs/*) never appear in the inventory; only top-level entry names (including directory basenames like 'docs') and an optional root notes.txt read are used.","severity":"medium"},{"evidence":"tests/core/test_moa_team.py:746-764 — asserts only: {s.persona}=={researcher,implementer}; (tmp_path/'r'/'research_notes.md').is_file(); payload mode consensus_then_team; panel_wrote False; 'researcher' in text and 'Writes' in text. seed_files={'notes.txt':'edge API'} at L751 with no content/tool_trace/reads checks. Grep of this file: only other tool_trace use is test_record_determination_false (L675), not researcher. Production path src/swarm/core/moa/team.py:294-308 researcher does list_files('.'), optional read_file('notes.txt'), embeds notes in body under '## Notes', write_file research_notes.md — unasserted by this test.","file":"/home/matthewh/open-swarm/tests/core/test_moa_team.py","fix_hint":"Assert researcher.tool_trace includes list_files('.') and read_file('notes.txt'); assert 'edge API' in research_notes.md and in result.reads.","issue":"test_researcher_specialist_and_payload only checks that research_notes.md exists and personas ran; it does not assert tool_trace contains list_files/read_file, that research_notes embeds seed notes 'edge API', or that result.reads records '.' / 'notes.txt'.","reason":"Shipped test only checks personas, that research_notes.md exists, payload mode/panel_wrote, and that format_team_text mentions researcher/Writes. It seeds notes.txt with 'edge API' but never asserts researcher tool_trace (list_files/read_file), notes content embedded in research_notes.md, or result.reads for '.' / 'notes.txt'. Implementation does perform those steps; the test simply does not lock them in. Low severity: coverage gap, not a production defect.","severity":"low"},{"evidence":"team.py implementer (L452-457): if (tools.root / \"moa_determination.md\").exists(): tools.read_file(\"moa_determination.md\"); trace.append(...); body uses det, not return value. Researcher (L428-438) only list_files/notes.txt and embeds det[:1500]. persona_swarm.WorkspaceTools.read_file L103-108: p.read_text can raise; _run_specialist L503-516 maps Exception to ok=False. det is always passed in-memory from run_moa_then_team L618-622.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Use the returned text in the body (or guard read with try/except and fall back to det); optionally have researcher also read_file('moa_determination.md') for consistent tool traces.","issue":"In the same _run_specialist tool surface, implementer calls read_file('moa_determination.md') and discards the content (audit/trace only) while the body uses the det parameter. An unreadable file would fail the specialist even though determination text is already available; researcher never reads that file via tools either.","reason":"Discarded audit read is real and can fail implementer if the file exists but is unreadable, despite det already available; researcher never tool-reads the file either. Practical impact is low: file is written from the same det on the default path, and missing file is skipped via exists().","severity":"low"},{"evidence":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py: researcher body uses det[:1500] at L436-438 (`f\"## MoA determination\\n{det[:1500]}\\n\\n\"`); implementer body uses full det at L455-457 (`f\"## MoA consensus\\n{det}\\n\\n\"`). Same function, same determination string; only research notes truncate at 1500 chars (tester also truncates at L468 with det[:1200]; docs uses full det at L481).","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Document the truncation or share one max length / full det consistently across specialists.","issue":"Researcher embeds det[:1500] while implementer embeds full det; long determinations are silently truncated only in research notes.","reason":"Independent read of shipped team.py confirms the claimed asymmetry: long MoA determinations are silently truncated in researcher output while implementer embeds the full determination. Real inconsistency, severity nit (template/content-budget quirk, not a correctness or security break).","severity":"nit"},{"evidence":"tests/core/test_moa_team.py:70-88 test_run_moa_consensus_only_no_writes:\n  assert result.writes == []  # L83\n  assert result.panel_wrote is False  # L84\n  opinions = result.moa_payload.get(\"opinions\") or []  # L87 — uses moa_payload for opinions only; no moa_payload.get(\"writes\")\n\nscripts/trace_moa_champagne.py:56-61:\n  checks.append(check(\"A.writes empty\", a.writes == [], str(a.writes)))\n  checks.append(check(\"A.panel writes empty\", (a.moa_payload.get(\"writes\") or []) == []))\n\nIndependence of surfaces (why missing assert is not pure style):\n- src/swarm/core/moa/team.py:62-65 panel_wrote always return False (does not read moa_payload writes)\n- team.py:251-258 run_moa_consensus returns writes=[] hardcoded while only logging moa_payload.get(\"writes\") (L245-249)\n- docs/examples/moa-consensus-vs-team/README.md:277-279: top-level writes = team I/O; moa.writes = panel act surface\n- src/swarm/core/moa/tools.py:42-50 consult_moa always act=False (so nested writes empty by construction today)","file":"/home/matthewh/open-swarm/tests/core/test_moa_team.py","fix_hint":"Add assert (result.moa_payload.get(\"writes\") or []) == [] in test_run_moa_consensus_only_no_writes and team contrast tests.","issue":"Unit tests assert panel_wrote is False and result.writes == [] but do not assert moa_payload['writes'] == [] (champagne script does).","reason":"Confirmed: test_run_moa_consensus_only_no_writes asserts only result.writes == [] and result.panel_wrote is False, never moa_payload writes. Champagne dual-checks both surfaces. Gap is real but low-impact: consult_moa always uses act=False, panel_wrote is hardcoded False, and consensus_only hardcodes writes=[].","severity":"low"},{"evidence":"team.py:391-395 writes seed then tools.writes.clear()/reads.clear(); result.writes=list(tools.writes) at team.py:465. persona_swarm.py:286-291 same clear with comment 'Reset traces so seed writes don't count as swarm work'. test_moa_team.py:187 seed_files notes.txt; :207 assert 'decision.md' in result.writes only. test_moa_team.py:751 seeds notes.txt with no writes exclusion. Grep: only not-in-result.writes uses are moa_determination.md and path-escape paths (lines 670,1058,1079,1154)—never seed keys. hybrid/orchestrator seeded tests similarly assert decision.md present + notes content on disk, not notes.txt not in writes.","file":"/home/matthewh/open-swarm/tests/core/test_moa_team.py","fix_hint":"Add assert \"notes.txt\" not in result.writes (and ideally not pre-seed noise in reads); optionally assert \"moa_determination.md\" in result.writes when record_determination is True.","issue":"No regression test for the seed write-tracking clear invariant: seeds are used but tests never assert seed paths are absent from result.writes. Removing tools.writes.clear()/reads.clear() would still pass current asserts that only check specialist paths are present (e.g. decision.md in result.writes).","reason":"Production code intentionally clears tools.writes/reads after seed_files so seeds do not count as swarm work; result.writes is list(tools.writes). Seeded tests only assert specialist paths are present (or notes.txt still on disk), never that seed keys are absent from result.writes. Removing clear() would leave current asserts green.","severity":"medium"},{"evidence":"tests/core/test_moa_agents_orchestrator.py:30 seed_files={\"notes.txt\": ...}; L53 comment '# Writes are specialist/orchestrator artifacts only'; L54-55 only `assert \"decision.md\" in result.writes` and `assert \"test_notes.md\" in result.writes` (no exclusion of notes.txt, no equality); L57-58 only content check `(ws / \"notes.txt\").read_text(...)`. Contrast src/swarm/core/moa/team.py:391-394 which writes seeds then `tools.writes.clear()`—the invariant the comment implies is not tested.","file":"/home/matthewh/open-swarm/tests/core/test_moa_agents_orchestrator.py","fix_hint":"Assert \"notes.txt\" not in result.writes to lock the delegated run_moa_then_team clear behavior.","issue":"Comment claims writes are specialist/orchestrator-only after seeding, but assertions only check positive membership of specialist artifacts, not exclusion of the seed path.","reason":"The test seeds notes.txt and comments that writes are specialist/orchestrator-only, but only asserts positive membership of decision.md/test_notes.md in result.writes and that notes content remains on disk. It never asserts notes.txt ∉ result.writes, set equality, or that ADR.md is listed. Implementation clears seed traces (tools.writes.clear() after seeding); removing that clear would still leave this test green, so the exclusivity claim is unenforced.","severity":"low"},{"evidence":"In /home/matthewh/open-swarm/src/swarm/core/moa/team.py run_moa_then_team: lines 429–436 build task_names via (specialist_tasks or []) then log task_names or [\"implementer(default)\"]. Empty list is falsy, so specialist_tasks=[] yields task_names=[] and the champagne start line logs tasks=['implementer(default)']. Lines 460–467 only inject the default implementer TeamTask when specialist_tasks is None; [] skips that branch. Lines 469–478 then for-loop over [], so no specialist runs; line 480 falls back final=det with specialists_ok=[]. Same None/empty collapse in the log path, different execution for [] vs None.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Log task_names as-is (or \"(none)\"); apply the same None-vs-empty policy in both the log and the defaulting branch.","issue":"Start log can claim tasks=['implementer(default)'] when specialist_tasks=[] while no default specialist is scheduled (default only if specialist_tasks is None). Misleading champagne start line vs actual work.","reason":"Concrete control-flow mismatch: start INFO uses falsy-or fallback that treats [] like 'will run default implementer', while scheduling uses identity check is None only. Passing specialist_tasks=[] is type-legal and intentionally means zero specialists, but logs claim implementer(default). Real observability bug (not style-only); medium severity as misleading ops/trace champagne line, not wrong file writes when callers pass None or non-empty tasks.","severity":"medium"},{"evidence":"src/swarm/core/moa/team.py:419-424: panel_writes = list(moa_payload.get(\"writes\") or []); logger.info(\"moa.team after_panel answer_len=%d panel_writes=%s (expect [])\", len(det), panel_writes) — INFO only, no if panel_writes. Same file:245-250: consensus_only done ... panel_writes=%s specialists=0 — also logger.info only. Same file:62-65: @property panel_wrote always returns False. No logger.warning/error on non-empty panel_writes anywhere under src/swarm/core/moa for this invariant.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"If panel_writes: logger.warning(\"moa.team champagne violation panel_writes=%s\", ...).","issue":"Champagne panel-write invariant is only INFO with \"(expect [])\"; non-empty panel_writes never promote to warning/error, so a broken collect/act linkage is easy to miss in logs.","reason":"Independent read of team.py confirms the champagne panel-write check is observability-only at INFO. In run_moa_then_team, after consult_moa, panel_writes is taken from moa_payload and logged with logger.info(\"... panel_writes=%s (expect [])\", ...); there is no branch that elevates non-empty panel_writes to warning/error or fails. In run_moa_consensus (consensus_only), the same list is only INFO-logged without even an expect annotation. Grep shows no other production panel_writes checks; MoATeamResult.panel_wrote is hardcoded False and never inspects payload writes. Claimed line numbers (507–511 / 318–321) are stale—actual sites are ~419–424 and ~245–250—but the behavior matches the finding.","severity":"medium"},{"evidence":"_run_specialist (/home/matthewh/open-swarm/src/swarm/core/moa/team.py): unknown purpose at 270–283 logs only `logger.warning(\"moa.team specialist unknown purpose=%r\", ...)` then returns PersonaResult(ok=False) before start. Start is 286–289 (`moa.team specialist start purpose=%s output_path=%s`). Success 350–354: `moa.team specialist done purpose=%s ok=True trace=%s`. except 362–370: `logger.exception(\"moa.team specialist failed purpose=%s\", purpose)` only—no done/ok=False pair—then return ok=False. run_moa_then_team 454–456: `specialists_ok=%s` with `[s.persona for s in specialist_results if s.ok]`. tests/core/test_moa_team.py test_unknown_specialist_purpose_ok_false (~605–625) asserts wizard.ok is False / output / files only; no caplog assertions on warning vs start/done.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Always emit specialist start; always emit specialist done ok=…; add specialists_failed= to consensus_then_team done.","issue":"Specialist lifecycle logging is incomplete on failure: unknown purpose has no start/done pair; exceptions log exception only (no done ok=False); done summary only lists specialists_ok successes so failed personas vanish from the closing champagne line.","reason":"Shipped _run_specialist and run_moa_then_team match the claim: unknown purpose warns and returns ok=False before any start/done; success emits specialist done ok=True; except only logger.exception (no done ok=False); consensus_then_team done sets specialists_ok via [s.persona for s in specialist_results if s.ok], so failed personas disappear from that line. test_unknown_specialist_purpose_ok_false only asserts result/files, not log shape. Line numbers in the claim are stale (~270–456 now) but the logic is the same. Severity medium is observability-only: failures are still returned in specialist_results and unknown/exception paths do log, just not as a consistent start/done lifecycle.","severity":"medium"},{"evidence":"src/swarm/core/moa/team.py:131-150 — for item in list: only TeamTask/dict/str; else ignored; `return tasks or None`. Nested str uses parse_team_tasks(item) (lines 146-149); nested list has no branch. run_moa_then_team lines 433-440: if specialist_tasks is None → default TeamTask implementer. blueprint_moa_orchestrator.py:84-92,119-124 passes parse result through. tests/core/test_moa_team.py:370-384 nested string; 397 empty list only. No `# silently skip` comment; lines 237-248 are consensus_only logging, not parse.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Recurse into nested lists, or log/raise on unsupported element types instead of silent drop.","issue":"Nested lists and other non-(str|dict|TeamTask) list elements are silently ignored; if every element is skipped, result is None and triggers the default-implementer path above.","reason":"Shipped parse_team_tasks list branch only accepts TeamTask/dict/str; nested lists and other element types fall through with no handling, and an empty collected list returns None. run_moa_then_team then substitutes a default implementer when specialist_tasks is None (blueprint/orchestrator path). Nested strings recurse; nested lists do not. Claimed line numbers/comment are outdated, but the behavior is current. CLI rejects empty parse results instead of defaulting.","severity":"medium"},{"evidence":"SPECIALIST_PURPOSES = frozenset({\"researcher\",\"implementer\",\"tester\",\"docs\"}) at team.py:27-35. parse_team_tasks (team.py:90-130) splits purpose:instruction[@path], only strips/non-empty filters, appends TeamTask(purpose=..., instruction=..., output_path=_DEFAULT_OUTPUT_PATHS.get(purpose.lower())) with no membership check — so unknown purposes parse (confirmed tests/core/test_moa_team.py:489 \"unknown:do\" → purpose unknown, and :430 \"unknown:do stuff\" in never-raises cases). _run_specialist (team.py:269-283): purpose = task.purpose.lower().strip(); if purpose not in SPECIALIST_PURPOSES: return PersonaResult(..., output=f\"unknown specialist purpose {task.purpose!r}; known: ...\", ok=False). run_moa_then_team (team.py:442-453) appends every _run_specialist result then final = specialist_results[-1].output if specialist_results else det — last unknown task makes final the error string. test_unknown_specialist_purpose_ok_false (tests/core/test_moa_team.py:605-625) asserts wizard ok is False and implementer still runs.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Optionally warn or filter unknown purposes in parse_team_tasks; when computing final prefer last ok specialist or fall back to determination.","issue":"Unknown purposes are accepted at parse time with no check against SPECIALIST_PURPOSES; failure is only a soft ok=False later in _run_specialist. final can become the error string if the last task is unknown.","reason":"parse_team_tasks never checks SPECIALIST_PURPOSES and builds TeamTask for any non-empty purpose (including unknown:do). Validation happens only in _run_specialist as a soft PersonaResult(ok=False) with an error message in output; the team loop still runs remaining tasks. final is specialist_results[-1].output, so when the last task has an unknown purpose, final becomes that error string rather than the MoA determination or a successful specialist write.","severity":"medium"},{"evidence":"team.py format_team_text L219-222: `specs = payload.get(\"specialists\") or []` then `if mode == \"consensus_only\" or not specs:` → \"(none — consensus only; no team writes)\". run_moa_then_team L460-468: `if specialist_tasks is None:` only defaults implementer; L470+ loops empty list; return mode remains \"consensus_then_team\". team_result_to_payload L185/L188-197 serializes mode + empty specialists list. swarm_cli rejects empty tasks before call (not tasks → Exit 2). No specialist_tasks=[] tests in repo.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Branch on mode first; for empty specialists use a mode-neutral message like “(none)” or “(no specialists scheduled)”.","issue":"format_team_text treats empty specialists as “consensus only” even when mode is consensus_then_team (empty list is allowed by run_moa_then_team, which only defaults when specialist_tasks is None).","reason":"Independent read confirms the issue: format_team_text uses `if mode == \"consensus_only\" or not specs` and always prints \"(none — consensus only; no team writes)\" when specialists are empty, regardless of mode; run_moa_then_team only substitutes a default implementer when specialist_tasks is None (is None), so specialist_tasks=[] returns mode=consensus_then_team with specialist_results=[] and team_result_to_payload feeds format_team_text that misleading path. Claimed line numbers are outdated (logic is now ~L219-222 and ~L460-468). Severity is lower than claimed: display-only, library edge case; CLI rejects empty --team-tasks.","severity":"medium"},{"evidence":"team.py team_result_to_payload L154-175: determination=result.determination (str), moa=result.moa_payload. format_team_text L178-212: det=payload.get('determination'); if dict use det.get('answer'); never references payload['moa'], opinions, or rationale. run_moa_consensus L245: det=(moa_payload.get('determination') or {}).get('answer') or ''. cli.py format_moa_text L197-217: loops opinions and prints det.get('rationale'). Demo JSON 05-demo-consensus-only.json keeps moa.opinions + moa.determination.rationale while top-level determination is the answer str. swarm_cli.py L489-494: team path uses format_team_text; non-team uses format_moa_text.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Optionally render moa.opinions and moa.determination.rationale in text, or document that text mode is intentionally determination+team only.","issue":"Human text path drops panel opinions and determination rationale that JSON keeps under moa (unlike format_moa_text). consensus_only text is effectively only the answer string.","reason":"Independent read of team.py confirms format_team_text only emits mode/question, top-level determination answer string, specialists, writes, and panel_wrote—never payload['moa']['opinions'] or determination.rationale. team_result_to_payload keeps full moa_payload under 'moa' while top-level determination is a bare str (answer only, extracted in run_moa_consensus). Sibling format_moa_text in cli.py prints each opinion and optional _rationale_. Claimed line numbers are outdated (format_team_text is ~178–212; team_result_to_payload ~154–175) but the behavior and asymmetry are real. Nuance: the answer string itself often already embeds a Panel digest from the orchestrator, so consensus_only text is not always a one-line claim, but structured opinions and rationale still exist only under JSON moa.","severity":"medium"},{"evidence":"TEAM_RESULT_PAYLOAD_KEYS includes \"reads\" and \"final_preview\" (/home/matthewh/open-swarm/src/swarm/core/moa/team.py L184-195). team_result_to_payload documents them (L254-257) and sets \"reads\": list(result.reads) and \"final_preview\": (result.final or \"\")[:800] (L288-290). format_team_text (L412-446) only emits MoA mode, Question, ## Determination, ## Specialists (+ optional tool_trace/output_preview[:300]), ## Writes, and panel_wrote=...; it never references payload[\"reads\"] or payload[\"final_preview\"]. consensus_only final=det (L492); consensus_then_team final=last specialist output (L687-701), so final_preview can differ from determination but still is omitted from text. tests/core/test_moa_team.py test_team_result_to_payload_and_format_consensus_only asserts payload[\"reads\"]==[] and isinstance(payload[\"final_preview\"], str) (L696-698) and only checks text for mode/question/determination/specialists/writes/panel_wrote (L702-712)—no text assertions for reads or final_preview. CLI: --json dumps full payload; non-json team path uses format_team_text (swarm_cli.py L485-492).","file":"/home/matthewh/open-swarm/src/swarm/core/moa/team.py","fix_hint":"Add ## Reads and a short final_preview section, or drop unused keys from the shared payload if text is the primary non-JSON surface.","issue":"Payload contract includes reads and final_preview, but format_team_text never surfaces them (JSON vs text mismatch for the same object).","reason":"Independently confirmed: the team JSON/trace payload contract includes reads and final_preview, while format_team_text is a human summary that never prints those keys (writes/panel_wrote are shown; reads/final_preview are not). This is a real JSON-vs-text surface gap, not a runtime correctness failure—text mode is documented as a summary and determination/specialist previews cover much of the same content—so impact stays low. Claimed line numbers L238-241/L245-279 are outdated relative to the current file.","severity":"low"},{"evidence":"cli.py L17-26: comment + leaf imports only (`from swarm.core.moa.backends/orchestrator/types`). tools.py L11: `from swarm.core.moa.cli import build_backend, run_moa_cli`. agents_orchestrator.py L25-32: imports team + `from swarm.core.moa.tools import consult_moa`. team.py L21: `from swarm.core.moa.tools import consult_moa`. __init__.py L43: `from swarm.core.moa.agents_orchestrator import run_moa_agents_orchestrator` (eager). Cycle path: __init__ → agents_orchestrator → (team →) tools → cli; package-root import in cli would close the cycle on incomplete package init.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/cli.py","fix_hint":"Keep cli/backends/orchestrator free of package-root imports; ideally move build_backend/run_moa_cli off the tools→cli path (e.g. shared leaf module) and add an import-smoke test for from swarm.core.moa import MoAOrchestrator and from swarm.core.moa.agents_orchestrator import build_moa_orchestrator_agents.","issue":"Latent package-level circular import still exists as a structural risk for anything that loads agents_orchestrator (including build_moa_orchestrator_agents). Prevention is only that cli imports leaf modules instead of swarm.core.moa; reintroducing a package-root import in cli would break package import again.","reason":"Independent read confirms a latent import cycle: package __init__ eagerly imports agents_orchestrator, which imports tools (and team which also imports tools); tools imports cli. cli currently imports only leaf modules (backends/orchestrator/types) and documents that avoiding package-root import prevents the cycle. Reintroducing `from swarm.core.moa import ...` (or other package-namespace deps defined later in __init__) during package load would re-enter incomplete __init__ and break import of agents_orchestrator / build_moa_orchestrator_agents / `import swarm.core.moa`. Not an active runtime failure today—structural fragility held only by import discipline.","severity":"medium"},{"evidence":"__init__.py L43: `from swarm.core.moa.agents_orchestrator import run_moa_agents_orchestrator` (before backends/orchestrator). agents_orchestrator.py L26-32 imports team + `from swarm.core.moa.tools import consult_moa`. tools.py L11: `from swarm.core.moa.cli import build_backend, run_moa_cli`. team.py L21 also imports consult_moa, so package import also hits tools→cli via the team re-exports at __init__.py L57-71. cli.py L17-18: `# Import from leaf modules (not package __init__) to avoid circular import: # __init__ → agents_orchestrator → tools → cli → __init__`. docs/examples/moa-orchestrator/README.md L449 troubleshooting row: `Circular import on from swarm.core.moa import … in CLI path` → `Prefer from swarm.core.moa.orchestrator import MoAOrchestrator`.","file":"/home/matthewh/open-swarm/src/swarm/core/moa/__init__.py","fix_hint":"Lazy/export-on-demand for agents_orchestrator symbols, or drop them from package __init__ and keep submodule imports only.","issue":"Package __init__ eagerly imports the agents_orchestrator surface (SpecialistTask, run_moa_agents_orchestrator), so ordinary from swarm.core.moa import … always traverses the agents_orchestrator→tools→cli chain even when callers only want orchestrator/backends.","reason":"Package __init__ still eagerly imports agents_orchestrator first, and that module pulls tools→cli (also team→tools→cli). cli.py and the moa-orchestrator example README still document the package-level cycle and recommend leaf imports. Minor claim nit: SpecialistTask is re-exported from team, not from agents_orchestrator in __init__.","severity":"low"}]

## Test summaries
[{"failures":["tests/core/test_moa_team.py::test_team_emits_info_logs - AssertionError: missing log token(s) ['panel_writes_n=0'] (actual log has panel_writes=[] specialists=0)"],"passed":false,"summary":"1 failed, 190 passed in 8.37s"},{"failures":[],"passed":true,"summary":"Both scripts exited 0: demo_moa_consensus_vs_team invariants_ok=True; trace_moa_champagne 10/10 checks passed (all champagne invariants held)."},{"failures":[],"passed":true,"summary":"Import graph OK: PYTHONPATH=src loads swarm.core.moa.* and swarm.core.swarm_cli.app. Nested-asyncio guards in moa/agents_orchestrator and persona_swarm work outside and inside a running loop (ThreadPoolExecutor + asyncio.run); consult_moa(fake) succeeds both ways. Bare asyncio.run remains in swarm_cli (5) and blueprint_utils BlueprintFunctionTool (1)—safe as CLI entrypoints, fails under a running loop."}]

## Fix attempts (worktree-isolated; may need merge)
[{"applied":true,"files_touched":["/home/matthewh/open-swarm/src/swarm/core/moa/team.py","/home/matthewh/open-swarm/tests/core/test_moa_team.py"],"summary":"Distinguish missing tasks (None → default implementer) from explicit zero specialists ([]). parse_team_tasks no longer maps empty/all-skipped input to None; run_moa_then_team defaults only on is None; tests and CLI empty-reject coverage updated.","tests_run":"pytest tests/core/test_moa_team.py::test_parse_team_tasks_empty_parts_with_pipes tests/core/test_moa_team.py::test_parse_team_tasks_empty_purpose_segments_dropped tests/core/test_moa_team.py::test_default_specialist_when_tasks_none tests/core/test_moa_team.py::test_empty_specialist_tasks_skips_default_implementer tests/core/test_moa_team.py::test_parse_team_tasks_string_and_at_path tests/cli/test_moa_command.py::test_swarm_cli_moa_team_empty_team_tasks_exit_2 — 10 passed; full test_moa_team.py 120 passed / 2 failed unrelated (backslash path //, contrast asset keys)"},{"applied":true,"files_touched":["/home/matthewh/.grok/worktrees/matthewh-open-swarm/subagent-019faa6f-b986-7b60-a8c5-54796f4b5db0/src/swarm/core/moa/backends.py","/home/matthewh/.grok/worktrees/matthewh-open-swarm/subagent-019faa6f-b986-7b60-a8c5-54796f4b5db0/src/swarm/core/moa/orchestrator.py","/home/matthewh/.grok/worktrees/matthewh-open-swarm/subagent-019faa6f-b986-7b60-a8c5-54796f4b5db0/tests/core/test_moa_leftovers.py"],"summary":"Live MoA backends now kill subprocesses on CancelledError from orchestrator wait_for, not only on their own TimeoutError. Added shared _kill_process helper; Grok and Acpx both clean up on cancel; Acpx also enforces Python-side timeout. Documented nested-timeout guidance on MoAOrchestrator.per_participant_timeout. Unit tests cover orchestrator-timeout cancel cleanup for grok/acpx and backend-local grok timeout.","tests_run":"pytest tests/core/test_moa_leftovers.py (8 passed); pytest tests/core/test_moa_grok_backend.py tests/core/test_moa.py -k 'backend or acpx or grok or timeout or consult' (7 passed, 11 deselected)"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/src/swarm/core/moa/team.py","/home/matthewh/open-swarm/tests/cli/test_moa_command.py","/home/matthewh/open-swarm/tests/core/test_moa_team.py","/home/matthewh/open-swarm/tests/integration/test_swarm_workflows_proof.py","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/assets/05-demo-consensus-only.json","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/assets/05-demo-consensus-then-team.json","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/assets/06-cli-team.json","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/README.md"],"summary":"Normalized team_result_to_payload so top-level determination is the same object as run_moa_cli/moa.determination (answer/rationale/participant_names/analysis), updated validate_team_payload, tests, and example assets so data[\"determination\"][\"answer\"] works with or without --team.","tests_run":"pytest tests/cli/test_moa_command.py tests/core/test_moa_team.py tests/integration/test_swarm_workflows_proof.py --no-cov → 154 passed"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/README.md","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/scripts/render_diagrams.sh","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/scripts/capture_example_runs.sh","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/assets/05-demo-contrast.json","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/assets/05-demo-stdout.txt","/home/matthewh/open-swarm/docs/examples/moa-consensus-vs-team/assets/04-trace-run.log","/home/matthewh/open-swarm/docs/SWARM_WORKFLOWS.md","/home/matthewh/open-swarm/scripts/demo_moa_consensus_vs_team.py","/home/matthewh/open-swarm/scripts/trace_moa_champagne.py","/home/matthewh/open-swarm/tests/core/test_moa_team.py"],"summary":"Renamed moa-consensus-vs-team Path A/B labels to consensus_only / consensus_then_team (result.mode ids) so they no longer collide with global SWARM_WORKFLOWS B (openai-agents persona swarm). Both modes are explicitly under global A (MoA). Updated README, SWARM_WORKFLOWS first-class row, demo/trace scripts, capture assets, diagram markers, and added regression test test_example_readme_does_not_reuse_global_workflow_b_label.","tests_run":"pytest tests/core/test_moa_team.py::test_example_readme_does_not_reuse_global_workflow_b_label tests/core/test_moa_team.py::test_example_contrast_asset_invariants tests/core/test_moa_team.py::test_run_moa_consensus_only_no_writes -q --no-cov → 3 passed"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/docs/examples/moa-orchestrator/README.md","/home/matthewh/open-swarm/tests/core/test_moa_agents_orchestrator.py","/home/matthewh/open-swarm/src/swarm/core/moa/team.py"],"summary":"Corrected moa-orchestrator README to describe A (MoA) then scripted TeamTask specialists via run_moa_then_team, with agent objects optional/inspection-only (not live model-B agent-as-tool). Added unit tests for scripted body + README wording; fixed pre-existing team.py backslash-string syntax error that blocked imports.","tests_run":"pytest tests/core/test_moa_agents_orchestrator.py -q --no-cov (6 passed)"},{"applied":true,"files_touched":["docs/examples/moa-consensus-vs-team/README.md","tests/core/test_moa_schema.py"],"summary":"Corrected README so consult_moa is documented as always no-act (hardcoded) with approve-reads permission, not as consult_moa(..., act=False). Aligned Path B mermaid label. Added unit test that signature has no act and passing act=False raises TypeError.","tests_run":"pytest tests/core/test_moa_schema.py -q --no-cov (5 passed)"},{"applied":true,"files_touched":["docs/examples/moa-orchestrator/README.md","docs/examples/moa-orchestrator/assets/diagram-specialists.mmd","docs/examples/moa-orchestrator/assets/diagram-architecture.mmd","docs/examples/moa-consensus-vs-team/README.md","tests/core/test_moa_agents_orchestrator.py"],"summary":"Reframed moa-orchestrator README so run_moa_agents_orchestrator is documented as agents-mode result shape over run_moa_then_team (name roster only); live Agent/Runner wiring pointed only at build_moa_orchestrator_agents; aligned sibling cross-link; extended unit tests for that contract (5 passed).","tests_run":"pytest tests/core/test_moa_agents_orchestrator.py -q --no-cov → 5 passed"},{"applied":true,"files_touched":["docs/examples/moa-orchestrator/README.md","docs/examples/moa-orchestrator/assets/diagram-architecture.mmd","docs/examples/moa-orchestrator/assets/diagram-specialists.mmd","docs/examples/moa-orchestrator/assets/diagram-optional-runner.mmd","docs/examples/moa-orchestrator/scripts/render_diagrams.sh","tests/core/test_moa_agents_orchestrator.py"],"summary":"Fixed moa-orchestrator README §1/§2.2 to document the real scripted path (run_moa_agents_orchestrator → run_moa_then_team / WorkspaceTools) instead of a non-run Agents coordinator + task_* loop; added optional §2.3 for build_moa_orchestrator_agents + Runner; synced .mmd assets and render script; added unit tests locking docs + delegation behavior.","tests_run":"pytest tests/core/test_moa_agents_orchestrator.py -q --no-cov → 6 passed"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/scripts/demo_moa_consensus_vs_team.py","/home/matthewh/open-swarm/tests/core/test_moa_team.py"],"summary":"Exit-gated neither_panel_wrote now checks a/b moa_payload writes (real panel act surface) instead of always-False panel_wrote; kept a.writes==[]; unit tests assert empty moa_payload writes on consensus and team paths.","tests_run":"pytest tests/core/test_moa_team.py::test_run_moa_consensus_only_no_writes tests/core/test_moa_team.py::test_consensus_vs_team_contrast tests/core/test_moa_team.py::test_example_contrast_asset_invariants -q (3 passed); demo script smoke exit 0"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/src/swarm/core/moa/team.py","/home/matthewh/open-swarm/tests/core/test_moa_team.py"],"summary":"After consult_moa in run_moa_then_team, soft panel failures (ok_count==0 / all opinions ok=False) now early-return with empty specialist_results and no determination/specialist writes, instead of always writing moa_determination.md and running R/W specialists. Added unit test for the ok_count=0 path.","tests_run":"pytest tests/core/test_moa_team.py::test_soft_panel_failure_skips_specialists_and_determination (+ multi_specialists, record_determination_false, consensus_vs_team_contrast) — all passed; full test_moa_team.py 120 passed / 2 pre-existing unrelated failures"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/src/swarm/core/moa/cli.py","/home/matthewh/open-swarm/tests/cli/test_moa_command.py"],"summary":"Added Path A consensus_only markers (mode/specialists/panel_wrote) to run_moa_cli and a Typer subprocess test that asserts pure-path consensus_only when --team is omitted (empty writes/specialists, panel_wrote False), mirroring team-path coverage.","tests_run":"pytest tests/cli/test_moa_command.py -q --no-cov (25 passed)"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/src/swarm/core/swarm_cli.py","/home/matthewh/open-swarm/tests/cli/test_moa_command.py","/home/matthewh/open-swarm/tests/integration/test_swarm_workflows_proof.py"],"summary":"Non-team CLI (act=False) now calls run_moa_consensus + team_result_to_payload so swarm-cli moa --json emits mode=consensus_only; --act still uses run_moa_cli. Added/updated CLI and integration tests for the Path A contract.","tests_run":"PYTHONPATH=src python -m pytest tests/cli/test_moa_command.py tests/integration/test_swarm_workflows_proof.py::test_workflow_a_swarm_cli_moa_entry --no-cov -q → 26 passed"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/src/swarm/core/moa/team.py","/home/matthewh/open-swarm/src/swarm/core/moa/agents_orchestrator.py","/home/matthewh/open-swarm/tests/core/test_moa_team.py"],"summary":"Team fake defaults now generate structured responses for every moa_participants seat (not only analyst/critic), so FakeParticipantBackend no longer fails custom seats when moa_fake_responses is omitted. agents_orchestrator reuses the same seat-aware _default_fakes. Added unit + consensus coverage for custom seats.","tests_run":"pytest tests/core/test_moa_team.py::test_default_fakes_are_valid_structured_json_proposals tests/core/test_moa_team.py::test_default_fakes_json_safe_for_adversarial_question_text tests/core/test_moa_team.py::test_default_fakes_score_with_schema_and_synthesize_primary tests/core/test_moa_team.py::test_run_moa_consensus_default_fakes_without_explicit_responses tests/core/test_moa_team.py::test_default_fakes_cover_custom_seat_names tests/core/test_moa_team.py::test_run_moa_consensus_custom_seats_without_explicit_fakes tests/core/test_moa_team.py::test_run_moa_then_team_default_fakes_feed_specialists -q (7 passed)"},{"applied":true,"files_touched":["/home/matthewh/.grok/worktrees/matthewh-open-swarm/subagent-019faa6f-b992-7ef3-9a8d-14b4fe56b586/src/swarm/core/moa/team.py","/home/matthewh/.grok/worktrees/matthewh-open-swarm/subagent-019faa6f-b992-7ef3-9a8d-14b4fe56b586/tests/core/test_moa_team.py"],"summary":"parse_team_tasks only treats last @ as output-path when the suffix is path-like (/ \\ ~ or known file extension); emails/handles stay in the instruction and purpose defaults apply. Added _looks_like_output_path and unit coverage.","tests_run":"pytest tests/core/test_moa_team.py -k 'parse_team_tasks or email or edge_shapes or at_path' → 91 passed; full tests/core/test_moa_team.py → 119 passed, 1 pre-existing unrelated fail (test_team_emits_info_logs panel_writes_n=0)"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/src/swarm/core/moa/team.py","/home/matthewh/open-swarm/tests/core/test_moa_team.py"],"summary":"Treat specialist_tasks=[] like None so the default implementer is installed (matching logs and parse_team_tasks/CLI empty→None). Parametrized unit test covers None and [].","tests_run":"pytest tests/core/test_moa_team.py::test_default_specialist_when_tasks_none_or_empty -q (2 passed)"},{"applied":true,"files_touched":["/home/matthewh/open-swarm/src/swarm/core/moa/team.py","/home/matthewh/open-swarm/tests/core/test_moa_team.py"],"summary":"Introduced default_output_path(purpose) as the single source of truth for specialist default paths and used it in parse helpers, default TeamTask construction, and _run_specialist; removed hardcoded path fallbacks. Added/adjusted unit tests covering helper, parse, None output_path, and default implementer.","tests_run":"pytest tests/core/test_moa_team.py -k 'default_output_path or specialist_none_output or default_specialist or empty_specialist or case_insensitive_default or string_and_at or list_of_dicts or multi_specialists or unknown_specialist' (9 passed); full tests/core/test_moa_team.py had 1 unrelated failure in test_validate_team_payload_accepts_live_serializer"},{"applied":false,"files_touched":[],"summary":"parse_team_tasks list-of-dicts and TeamTask object tests already exist (test_parse_team_tasks_list_of_dicts, test_parse_team_tasks_list_of_team_task_objects) and both pass; no file changes. Full tests/core/test_moa_team.py: 106 passed, 9 failed (pre-existing mismatches with current parse_team_tasks/log behavior, outside requested scope).","tests_run":"pytest tests/core/test_moa_team.py (106 passed, 9 failed); list_of_dicts + list_of_team_task_objects both PASSED"},{"applied":false,"files_touched":[],"summary":"test_unknown_specialist_purpose_ok_false already present in tests/core/test_moa_team.py; no edit needed. Targeted test passed; full file had 106 passed / 9 unrelated failures.","tests_run":"pytest tests/core/test_moa_team.py (106 passed, 9 failed unrelated); test_unknown_specialist_purpose_ok_false PASSED"},{"applied":false,"files_touched":[],"summary":"test_swarm_cli_moa_verbose_without_team_exits_0 already present in tests/cli/test_moa_command.py with XDG isolation (xdg_root=tmp_path/xdg); no edit needed; do not commit.","tests_run":"pytest tests/cli/test_moa_command.py::test_swarm_cli_moa_verbose_without_team_exits_0 -v → PASSED"},{"applied":false,"files_touched":[],"summary":"README already documents swarm-cli moa --team/--workdir/--team-tasks; FEATURE_STATUS.md is a historical audit and should not get a live moa --team note. No changes.","tests_run":"none"}]
