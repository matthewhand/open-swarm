# Archived REQ transcripts

**Historical.** These files are the former full Intent / Success /
Constraints / Owner transcripts that used to live under
`docs/requirements/`. GitHub Issues are the REQ source of truth.

Live pointers (Issue URL only, where an Issue still exists):
[../../requirements/](../../requirements/).

The stale backlog index that hard-coded “PR N — in flight” statuses and a
guest live-preview host is [BACKLOG-INDEX.md](./BACKLOG-INDEX.md).

`operator/` is the Agent Router track’s original REQ-7…15. Those IDs later
collided with the product backlog; they are not pointers.
